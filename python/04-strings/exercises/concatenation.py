def concat(a: str, b: str) -> str:
    """Concatenate two strings using the + operator."""
    pass


def join_str(sep: str, parts: list) -> str:
    """Join the list of strings with the separator using join()."""
    pass
