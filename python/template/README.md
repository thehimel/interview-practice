# Topic Name

> **Template:** Copy this directory to create a new topic (e.g. `cp -r template 03-topic-name`). Then:
> 1. Rename `topic.py` to the topic name in both `solutions/` and `exercises/` (e.g. `foo.py` for topic "foo")
> 2. Rename `test_topic_solution.py` and `test_topic_exercise.py` to `test_<slug>_solution.py` and `test_<slug>_exercise.py`
> 3. Replace `"template"` and `"topic"` with the new directory and module names in both test files
> 4. Update `EXPECTED_OUTPUT` and test logic in the solution test file as needed

## File Structure

- `solutions/` – Reference implementation and solution tests
- `exercises/` – Stub for practice and exercise tests

## Problem Statement

Describe the problem here.

**Requirements:**

- Requirement 1
- Requirement 2

## Example

**Input:** Describe input format

**Output:**

```
Expected output
```

## Resources

* Add relevant links
