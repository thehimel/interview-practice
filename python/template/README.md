# Topic Name

> **Template:** Copy this directory to create a new topic (e.g. `cp -r template 03-topic-name`). Then:
> 1. Rename `test_topic_solution.py` and `test_topic_exercise.py` to `test_<slug>_solution.py` and `test_<slug>_exercise.py` (e.g. `test_foo_solution.py`)
> 2. Replace `"template"` with the new directory name (e.g. `"03-topic-name"`) in both test files

## Problem Statement

Describe the problem here.

**Requirements:**

- Requirement 1
- Requirement 2

## Example

**Input:** Describe input format

**Output:**

```
Expected output
```

## Resources

* Add relevant links
